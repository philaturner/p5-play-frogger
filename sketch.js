
var GRID_SIZE = 64;
var COLS = 8;
var ROWS = 7;

function setup(){
  var canvas = createCanvas(COLS * GRID_SIZE ,ROWS * GRID_SIZE);
  canvas.parent('canvas-container');
}

function draw(){
  background(61);
}
